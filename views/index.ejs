<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Supermarket Inventory</title>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>Supermarket Inventory</h2>          
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Product ID</th>
                    <th>Product Name</th>
                    <th>Quantity</th>
                    <th>Price</th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>              
                <% for (let i = 0; i < products.length; i++) { %>
                    <tr>
                        <td><%= products[i].id %></td>
                        <td><a href="products/<%= products[i].id %>" > <%= products[i].productname %> </a>  </td>
                        <td><%= products[i].quantity %></td>
                        <td><%= products[i].price %></td>
                        <td><a href="products/<%= products[i].id %>/edit" > Edit </a>  </td>
                        <td><a href="products/<%= products[i].id %>/delete">Delete</a></td>
                    </tr>
                <% }; %>               
            </tbody>
        </table>
    </div>
</body>
</html>